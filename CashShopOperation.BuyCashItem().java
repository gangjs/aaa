else if (action == 5) { // Wishlist  1.2.65 OK
            // 찜목록 여기 소스 켜버리면 꼬여서 에러생김
            /*
            MapleQuestStatus point = chr.getQuestRecord(58585);
            if (point.getCustomData() == null) {
                point.setCustomData("0");
            }
            int point2 = java.lang.Integer.parseInt(point.getCustomData());
            if (point2 > 9) {
                c.getPlayer().dropMessage(1, "캐시샵 재접속 이후 재시도 해주십시오.");
                doCSPackets(c);
                return;
            } else {
                point.setCustomData((point2 + 1) + "");
            }
            */

  ...
}
